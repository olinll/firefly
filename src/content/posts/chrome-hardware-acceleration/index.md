---
title: Chrome硬件加速功能开启与关闭
slug: chrome-hardware-acceleration
published: 2025-03-31 00:00:00
updated: 2025-03-31 00:00:00
description: 硬件加速功能可以提高 Chrome 浏览器的性能，但在某些情况下可能会导致问题。本教程将介绍如何打开或关闭 Chrome 硬件加速功能。
image: api
category: 站点
tags: ["Chrome", "硬件加速", "运维技巧"]
draft: false
# pinned: false
---

Chrome 浏览器的「硬件加速」功能通过利用计算机的 GPU 来提升页面渲染，并释放 CPU 资源。但是，如果与显卡驱动不兼容的话，就可能会导致 Chrome 运行异常（例如黑屏和卡顿），只能关闭它才能恢复正常工作。

## Chrome 硬件加速有什么用

在 Chrome 浏览器中，硬件加速又叫图形加速，它可以利用计算机的 GPU 来处理图形密集型任务，比如视频播放、游戏以及复杂的数学运算等。通过硬件加速，可以将这些任务从 CPU 中分离出来，让 CPU 能够更高效地处理其他事务，从而提升系统整体性能和响应速度。

但在某些情况下，硬件加速可能会引起 Chrome 浏览器的运行问题，如响应迟缓，甚至程序挂起或崩溃。还有反馈称，它可能会导致笔记本耗电量大大增加。如果你遇到类似问题，怀疑硬件加速是罪魁祸首，最好的办法是禁用它，看看是否能解决问题。

## 打开或关闭 Chrome 图形加速

1. 打开 Google Chrome 浏览器，点击右上角的汉堡（三个点）图标，选择「设置」。
2. 在「系统」选项中，打开或关闭「使用图形加速功能（如果可用）」开关。
3. 重启 Chrome 浏览器，以应用更改。

## 强制启用 Chrome 的 GPU 加速

如果 Chrome 硬件加速没有如预期那样发挥作用，可以尝试覆盖内置的软件渲染列表，在不受支持的系统上强制启用 GPU 加速：

1. 打开 Google Chrome 浏览器，在地址栏中执行 <chrome://flags/#ignore-gpu-blocklist>，
2. 将「Override software rendering list」设置为「Enable」启用状态。
3. 点击「Relaunch」重启 Chrome 浏览器。该选项可以忽略 Chrome 默认的 GPU 黑名单，从而在某些不被推荐的系统上强制启用 GPU 加速。

## 查看 Chrome 硬件加速状态

我们可以通过一个简单的步骤来检查硬件加速功能是否已经启用：

1. 打开 Google Chrome 浏览器，在地址栏中执行`chrome://gpu/`。
2. 在「Graphics Feature Status」区域，查看各项功能是否已经开启了硬件加速。

通常情况下，大多数功能都应该处于「Hardware accelerated」状态，表示硬件加速正常工作；如果发现有功能被禁用（Disabled），可能需要进一步检查硬件或驱动程序的设置，以确保图形加速能够正确启用。

建议你根据实际需求和电脑配置，选择启用或禁用 Chrome 硬件加速。

## 编辑建议

> 以下建议基于本条目内容生成，仅供发布前参考。

### 文章内容建议
- **缺少 Edge / Firefox 的对应方法**：本文只覆盖 Chrome，但 Edge 同样有"硬件加速"开关（同 chromium 内核）、Firefox 也有 `webgl.disabled` 等；建议加"## 附：其他浏览器的硬件加速"小节，覆盖主流 3 个浏览器
- **缺少"硬件加速导致的常见问题排查清单"**：开篇提到"黑屏、卡顿、耗电大"三种症状，建议补一节"## 三、症状-对策速查"做一张表格（症状 → 检查项 → 解决方案）
- **缺少 GPU 驱动排查流程**：`chrome://gpu/` 只能看 Chrome 内部状态，但底层驱动问题（驱动崩溃、设备管理器黄叹号）Chrome 看不出来；建议加"## 四、驱动层排查"引导读者用 `dxdiag`（Windows）或 `nvidia-smi`/`glxinfo`（Linux）
- 第二章"打开或关闭 Chrome 图形加速"的开关名"使用图形加速功能（如果可用）"是 **旧版 Chrome 命名**；新版 Chrome（110+）已改名为"使用硬件加速"或"使用图形加速"。建议在文首加"⚠️ 较旧 Chrome 界面"备注

### 修改建议
- 标题"Chrome硬件加速功能开启与关闭"为**纯说明性标题**，缺吸引力；建议改为"Chrome 硬件加速导致黑屏？3 招解决"或"一文搞懂 Chrome 硬件加速：开启/关闭/状态查看/强制启用"
- "## Chrome 硬件加速有什么用"章节标题应改为"## 一、硬件加速的作用"以保持章节编号一致
- frontmatter `image: "api"` 缺封面图，建议补一张"chrome://gpu/ 状态截图"或"Chrome 黑屏/正常对比图"

### 合并建议
- 候选合并对象：`video-compression`
- 合并理由：均涉及"硬件加速"主题（GPU 加速 NVENC/QSV/AMF）
- 综合判断：建议**保留独立**——本文是"浏览器层 GPU 开关"，video-compression 是"视频编码层 GPU 加速"，层级差异大；但应在文末增加"## 相关主题"区块互引

### slug 建议
- 当前：`chrome-hardware-acceleration`
- 建议：保留
- 理由：slug 准确，搜索"chrome 硬件加速"关键词命中率高，且符合英文/中文混合命名风格

### 分类建议
- 建议归类到：**随笔**（不太准确）或 **系统**
- 理由：本文是"浏览器配置"小技巧，归"技术插曲与避坑"是合理的；但从内容深度看更像"系统调优"——可考虑单独建"桌面工具"分类或并入"开发"

### tags 建议
- 建议：`[Chrome, 硬件加速]`
- 与现状对比：`[Chrome, 硬件加速, 运维技巧]`，差异说明：去掉泛词 `运维技巧`（所有文章都可加），保留 2 个核心 tag 更精准

### 其他建议
- 配图建议：可补一张"chrome://gpu/ 的 Graphics Feature Status 列表正常 vs 异常对比"截图
- 版本时效性建议：Chrome flag `ignore-gpu-blocklist` 在 Chrome 120+ 已部分废弃，建议加"截至 Chrome 120 验证"备注，并给出新版替代（如 `--enable-features=Vulkan` 命令行参数）
